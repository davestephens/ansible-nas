---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - name: Include vars
      include_vars:
        file: ../../defaults/main.yml

    - name: Try and stop and remove Hello World
      docker_container:
        name: "{{ beets_container_name }}"
        state: absent
      register: result

    - name: Check if Beets is stopped
      assert:
        that:
          - not result.changed
